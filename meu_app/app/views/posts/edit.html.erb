<% content_for :title, "Editar Post" %>

<div class="mb-6 flex items-center justify-between">
  <h1 class="text-2xl font-bold">Editar Post</h1>
  <div class="flex gap-3">
    <%= link_to "Ver", @post, class: "btn-secondary" %>
    <%= link_to "Voltar", posts_path, class: "btn-secondary" %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", post: @post %>
  </div>
</div>
