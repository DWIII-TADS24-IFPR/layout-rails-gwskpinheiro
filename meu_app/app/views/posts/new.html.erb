<% content_for :title, "Novo Post" %>

<div class="mb-6 flex items-center justify-between">
  <h1 class="text-2xl font-bold">Novo Post</h1>
  <%= link_to "Voltar", posts_path, class: "btn-secondary" %>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", post: @post %>
  </div>
</div>
